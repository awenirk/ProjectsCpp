#include <iostream>
using namespace std;

int main()
{
    // 1.Введіть три числа і виведіть на екран їх значення та значення суми цих чисел.
    /*
    int num1;
    int num2;
    int num3;
    int sum;
    cout << "Enter first num: ";
    cin >> num1;
    cout << "Enter second num: ";
    cin >> num2;
    cout << "Enter third num: ";
    cin >> num3;
    sum = num1 + num2 + num3;
    cout << "Nums: " << num1 << " " << num2 << " " << num3 << endl;
    cout << "Numders sum: " << sum;
    */

    // 2.Напишіть програму, яка обраховує середнє арифметичне двох чисел.
    /*
    int num1;
    int num2;
    int mean;
    cout << "Enter first num: ";
    cin >> num1;
    cout << "Enter second num: ";
    cin >> num2;
    mean = (num1 + num2) / 2;
    cout << "Nums: " << num1 << " " << num2 << endl;
    cout << "Numders mean: " << mean;
    */

    // 3.Напишіть програму, яка переводить кілометри в метри. Кілометри вводяться з клавіатури.
    /*
    int mInKm = 1000;
    int Km;
    int M;
    cout << "Enter Km: ";
    cin >> Km;
    M = Km * mInKm;
    cout << "In meеers: " << M << endl;
    */

    // 4.Ви купили в магазині 4 різні товари.
    // Дані ціни кожного товару (константні значення) та кількість товару, який був придбаний. 
    // Обрахувати та вивести вартість покупки.
    /*
    const int price1 = 200;
    const int price2 = 175;
    const int price3 = 140;
    const int price4 = 120;

    int count1;
    int count2;
    int count3;
    int count4;

    cout << "Enter count of first item: ";
    cin >> count1;
    cout << "Enter count of second item: ";
    cin >> count2;
    cout << "Enter count of third item: ";
    cin >> count3;
    cout << "Enter count of fourth item: ";
    cin >> count4;

    int sum = (price1 * count1) + (price2 * count2) + (price3 * count3) + (price4 * count4);
    cout << "Sum -> " << sum << endl;
    */
}

